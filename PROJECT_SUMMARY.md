# Family Health Monitor 功能与设计总结

更新时间：2026-07-03

本文档用于记录当前版本已经完成的功能、产品设计逻辑、技术接入状态，以及后续可继续讨论的问题。后面你可以直接基于这份文档提出修改建议。

## 1. 当前产品定位

这是一个面向家庭成员的小规模健康记录与关心 App。

核心目标：

- 让老人或家庭成员能方便记录血压、血糖、血脂等健康数据。
- 让同一个家庭/群组成员在授权范围内查看彼此的健康趋势。
- 用更温和、人本化的方式提醒和关心，而不是制造焦虑。
- 第一版优先适合少量亲友测试，不追求大规模公开上线。

## 2. 登录与账号设计

### 当前登录方式

- 使用 Firebase Anonymous Auth 匿名登录作为后台身份。
- 不使用手机号验证码。
- 不发送短信验证码，因此不会产生短信验证码费用。
- 用户在 App 内填写：
  - 我的称呼
  - 手机号

手机号只作为“换手机找回数据的索引”，不是 Firebase 的登录凭证。

### 找回数据逻辑

当前设计为：

```text
用户输入手机号
→ Firestore 查询 recovery_phone
→ 找到对应用户ID
→ 将当前匿名身份关联/指向旧用户资料
```

同时保留内部用户ID：

- 用户ID是系统内部稳定编号。
- 手机号适合用户记忆和找回。
- 用户ID适合作为后端稳定主键和兜底。
- 首页不突出显示用户ID，只放在“账号与找回”小入口中。

### UI 处理

首页已将原本较抢眼的“我的用户ID”大卡片改成小卡片：

- 标题：账号与找回
- 首页只显示手机号后 11 位，不显示 `+86`
- 点开后底部弹层显示：
  - 找回手机号
  - 用户ID
  - 恢复码
  - 复制找回信息按钮

## 3. 家庭/群组功能

### 我的家庭

用户进入后会看到当前家庭/群组。

已实现：

- 创建家庭/群组
- 加入家庭/群组
- 家庭邀请码
- 复制邀请码
- 更换邀请码
- 群组成员列表
- 成员昵称/关系备注

### 家庭邀请码位置

邀请码已经放在首页较明显位置，不再藏在群组设置里。

设计理由：

- 邀请码是拉家人进群的高频操作。
- 老年用户或不熟悉 App 的用户不适合在设置里找入口。

## 4. 健康记录功能

### 已支持记录类型

当前支持：

- 血压
  - 收缩压
  - 舒张压
- 血糖
  - 空腹血糖
  - 餐后血糖
  - 餐后 2 小时血糖等维度
- 血脂
  - 总胆固醇
  - 甘油三酯
  - 低密度脂蛋白
  - 高密度脂蛋白

记录时可以只填写本次实际测量的项目，不需要每次全部填满。

### 日期与表单

已完成：

- 记录日期选择器中文化。
- 时间显示为中文用户更容易理解的形式。
- 备注字段可选。
- 可选择这条记录是否只自己可见，或分享到某些群组。

### 记录真实性原则

当前图表逻辑遵循：

- 不为了画图而平均掉真实数据。
- 一天多次记录时，不简单取平均值。
- 如果当天有异常值，图上优先展示异常值。
- 滑动/点选对应日期时，应能查看当天多次测试结果。

设计理由：

健康记录的价值在于真实，而不是为了图表平滑牺牲信息。

## 5. 健康趋势功能

### 趋势范围

当前支持：

- 7 天
- 30 天
- 90 天
- 全部
- 自定义区间

自定义区间限制：

- 最大不超过 90 天
- 支持按月份或日期范围查看

### 图表指标选择

已实现：

- 每类趋势图可以选择显示哪些指标。
- 可以选择是否显示参考范围。
- 避免血脂这类多指标图表过于杂乱。

例如血脂指标较多时，用户可以只勾选关心的项目。

### 参考范围

已实现：

- 图中可显示浅绿色参考范围带。
- 参考范围可开关。
- 文案会说明各指标常见参考范围。

当前参考范围主要用于直观提示，不作为医学诊断。

### 横置查看

已实现横置查看入口。

设计目标：

- 在手机端可像看股票走势图一样横屏查看趋势。
- 对 30 天、90 天这类点较多的图更友好。

### 全部数据策略

对于超过 90 天的长期数据，当前思路是：

- 不强行每天画完整点。
- 后续可采用月度/年度聚合视图。
- 保留真实明细，图表只做长期概览。

## 6. 健康总结功能

趋势页顶部有总结卡片。

已实现：

- 显示当前范围内记录次数。
- 显示包含指标总数。
- 显示常见范围内数量和需留意数量。
- 以更直观形式展示分类统计，例如：
  - 血压：1/3
  - 血糖：2/6
  - 血脂：1/3

并补充解释：

```text
1/3 表示 3 项检测指标里有 1 项在常见范围内。
```

总结语气采用温和提示：

- 建议稍后复测
- 如果持续变化明显或感到不适，可以咨询专业人员

避免直接制造恐慌。

## 7. 记录明细与日历视图

已实现：

- 短期记录可直接查看明细。
- 30 天以上记录使用更紧凑的日历/月度视图。
- 日期有记录时可点击查看或修改。
- 没有数据的日期不可点，避免误导。

设计理由：

长期记录如果全部列表展示，会太冗长；日历式入口更适合查看历史。

## 8. 示例模块

已保留独立“示例模块”。

位置：

- 首页中作为一个小模块入口
- 不混在用户真实趋势图下面

功能：

- 构造 90 天模拟数据
- 展示长期使用后的趋势效果
- 方便新用户理解 App 能看到什么
- 方便演示和测试图表设计

正式 APK 中：

- 保留示例模块
- 不保留当前本机测试数据

## 9. 群组成员健康查看

已实现：

- 同群组成员主动分享健康记录后，其他成员可看到近况。
- 点击成员/趋势入口，可以像查看自己一样查看对方健康趋势。
- 只查看对方已分享给当前群组的数据。

群组近况限制：

- 首页“群组近况”最多显示几条，避免无限拉长首页。
- “最近的关心”也限制显示数量。

## 10. 关心功能

已实现基础关心互动：

- 给成员发送关心
- 查看最近关心
- 可回复/回应
- 可显示已读状态，后续可设置隐藏

设计目标：

- 强调家人之间的温和陪伴
- 不把 App 做成冰冷的指标监控工具

## 11. Firebase 接入状态

### 已接入

当前项目已接入 Firebase Android 配置：

- `android/app/google-services.json`
- Google Services Gradle 插件
- Firebase Auth
- Cloud Firestore

Android 包名：

```text
com.familyhealth.family_health_monitor
```

Firebase 项目信息：

```text
Project ID: littlecare-83003
Project Number: 451084972347
```

Firestore 存储区域：

```text
香港
```

### 当前登录方案

需要在 Firebase Console 开启：

```text
Authentication → Sign-in method → Anonymous
```

不需要开启：

```text
Phone 电话登录
```

### 当前规则状态

本地 `firestore.rules` 已改成测试规则：

```js
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

原先更严格的正式规则已备份：

```text
firestore.rules.strict.backup
```

注意：由于 Firebase CLI 登录在当前环境中失败，Firestore 规则最终是否发布成功，需要以 Firebase Console 页面为准。

## 12. APK 构建状态

已成功构建真实 Firebase debug APK：

```text
C:\Users\xiang\Desktop\workshop\build\app\outputs\flutter-apk\app-debug.apk
```

构建参数：

```text
--dart-define=USE_FIREBASE=true
```

说明：

- 这个 APK 使用真实 Firebase。
- 不使用本机演示数据。
- 适合小范围真机测试。
- 当前是 debug APK，不适合正式上架。

Release APK 曾尝试构建，但耗时过长，中途停止。后续等真机 Firebase 流程跑通后，再专门处理 release 签名和正式包。

## 13. 已做的错误诊断改进

之前登录失败只显示：

```text
暂时无法登录，请稍后再试
```

已改进为显示具体 Firebase 错误。

例如：

- `admin-restricted-operation`
- `operation-not-allowed`
- `network-request-failed`
- `permission-denied`
- `unavailable`

当前真机曾出现：

```text
Firebase 登录失败：admin-restricted-operation
```

判断原因：

- Firebase Anonymous 匿名登录未开启，或项目后台限制了匿名登录。

处理方式：

```text
Firebase Console → Authentication → Sign-in method → Anonymous → Enable
```

## 14. 中国大陆使用风险

由于 Firebase 属于 Google 服务体系，中国大陆网络环境下可用性不保证。

即使：

- Firestore 选香港
- 手机开启科学上网

也仍可能遇到：

- App 没有走代理
- Firebase Auth 无法连接
- Firestore 连接不稳定
- 不同手机/网络环境表现不同

当前建议：

1. 先用 Firebase 香港区域做少量真机测试。
2. 如果亲友网络环境可用，可以继续小范围使用。
3. 如果大陆网络不稳定，后续考虑迁移国内后端：
   - 腾讯云 CloudBase
   - 阿里云
   - LeanCloud 国内版
   - 自建后端 + 国内数据库

## 15. 当前仍待验证的问题

真机侧还需要继续验证：

- Anonymous 匿名登录开启后是否能成功进入。
- 新用户是否能创建资料。
- Firestore 是否能生成 `users`、`groups`、`health_records` 等集合。
- 创建家庭是否成功。
- 邀请码加入是否成功。
- 健康记录是否能云端保存。
- 多设备/换手机能否通过手机号找回。
- 群组成员是否能查看分享趋势。
- 中国大陆不同网络环境下稳定性如何。

## 16. 后续建议任务

### 短期

- 确认 Firebase Anonymous 已开启。
- 发布临时 Firestore 测试规则。
- 用新版 APK 真机测试全流程。
- 若失败，根据 App 红色错误继续定位。

### 中期

- 补正式 Firestore 安全规则。
- 做 release 签名 APK。
- 增加版本号与应用名称/图标。
- 加入更清晰的网络失败提示。
- 增加“如何换手机找回”的用户引导。

### 长期

- 评估是否迁移国内云服务。
- 增加数据导出/备份。
- 增加家庭成员权限分级。
- 增加更完整的健康档案。
- 根据年龄、性别、身高、体重等信息细化参考范围。

## 17. 重要设计原则

当前版本遵循以下原则：

- 记录真实，不为了画图牺牲真实。
- 趋势辅助理解，不替代医学诊断。
- 让老人用户看得懂、点得动、少迷路。
- 低频但重要的信息收起来，不打扰日常使用。
- 示例模块保留，但真实用户数据从空白开始。
- 第一版先服务少量可信用户，后续再加强安全和扩展性。
